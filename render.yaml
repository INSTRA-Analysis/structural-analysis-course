services:
  - type: web
    name: instrahub
    env: python
    rootDir: instra-web
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn --chdir instra-web app:app
    plan: free
